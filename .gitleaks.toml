# Gitleaks configuration
# https://github.com/gitleaks/gitleaks

[extend]
useDefault = true

[allowlist]
description = "Allowlist for NERV Systems"

# HubSpot Portal IDs and Form IDs are public identifiers, not secrets
regexes = [
  '''NEXT_PUBLIC_HUBSPOT_PORTAL_ID=\d+''',
  '''NEXT_PUBLIC_HUBSPOT.*FORM_ID=[a-f0-9-]+''',
  '''portalId.*\d{8,9}''',
  '''formId.*[a-f0-9-]{36}''',
]

# Ignore example/template files and historical diagnostic docs that only
# contained NEXT_PUBLIC_* HubSpot form IDs (deliberately client-bundled, not secrets)
paths = [
  '''\.env\.local\.example''',
  '''ENV-SETUP\.md''',
  '''FORM_DIAGNOSTIC\.md''',
]
